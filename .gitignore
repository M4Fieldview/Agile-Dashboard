# Generated by .github/scripts/fetch_jira.py and published straight to GitHub
# Pages as a deployment artifact. Deliberately not committed: it is 1.25 MB and
# rewritten every few minutes, so tracking it added 1-2 MB/day to git history
# forever. A committed copy would also be permanently stale and look
# authoritative. Fetch a fresh one for local work:
#   curl -o data.json https://m4fieldview.github.io/Agile-Dashboard/data.json
data.json

__pycache__/
*.pyc

# Local editor / agent state
.claude/
